[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "predictive-maintenance"
version = "0.1.0"
description = "Project scaffold for a predictive maintenance MSc thesis."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
